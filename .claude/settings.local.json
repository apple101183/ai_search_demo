{
  "permissions": {
    "allow": [
      "Bash(py --version)",
      "Bash(where python*)",
      "PowerShell($env:PATH -split ';' | Where-Object { $_ -ne '' } | Select-Object -First 30)",
      "PowerShell($excel = New-Object -ComObject Excel.Application -ErrorAction SilentlyContinue; if \\($excel\\) { \"Excel COM available\" } else { \"No Excel COM\" })"
    ]
  }
}
